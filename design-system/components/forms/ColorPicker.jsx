import React from "react";
import { ColorPicker as Impl } from "./_impl.js";

/** ColorPicker — Figma layer "Color picker". See ColorPicker.prompt.md. */
export function ColorPicker(props) {
  return React.createElement(Impl, props);
}
