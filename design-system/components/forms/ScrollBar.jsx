import React from "react";
import { ScrollBar as Impl } from "./_impl.js";

/** ScrollBar — Figma layer "Scroll bar". See ScrollBar.prompt.md. */
export function ScrollBar(props) {
  return React.createElement(Impl, props);
}
