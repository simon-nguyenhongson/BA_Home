import React from "react";
import { DateCalendarDropdown as Impl } from "./_impl.js";

/** DateCalendarDropdown — Figma layer "Date/Calendar dropdown". See DateCalendarDropdown.prompt.md. */
export function DateCalendarDropdown(props) {
  return React.createElement(Impl, props);
}
