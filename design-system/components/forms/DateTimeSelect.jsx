import React from "react";
import { DateTimeSelect as Impl } from "./_impl.js";

/** DateTimeSelect — Figma layer "Date/Time select". See DateTimeSelect.prompt.md. */
export function DateTimeSelect(props) {
  return React.createElement(Impl, props);
}
