Compact date item used by the picker's shortcut rail.

For preset ranges such as this month or last quarter.

```jsx
<DatePickerdateItem />
```

Props
- `selected` — boolean
- `state` — `default` | `focused` | `hover`
