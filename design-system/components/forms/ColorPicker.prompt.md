Colour field showing a swatch plus its value — 5 states.

Offer a curated palette, not a free spectrum picker.

```jsx
<ColorPicker />
```

Props
- `state` — `default` | `hover` | `filled` | `focused` | `disabled`
