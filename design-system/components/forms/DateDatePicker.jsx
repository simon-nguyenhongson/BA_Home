import React from "react";
import { DateDatePicker as Impl } from "./_impl.js";

/** DateDatePicker — Figma layer "Date/Date picker". See DateDatePicker.prompt.md. */
export function DateDatePicker(props) {
  return React.createElement(Impl, props);
}
