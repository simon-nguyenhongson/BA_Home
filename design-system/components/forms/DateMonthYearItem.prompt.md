Month or year cell in the month / year panels.

Used by the month, quarter and year calendar types.

```jsx
<DateMonthYearItem />
```

Props
- `selected` — boolean
- `hovered` — boolean
- `single` — boolean
- `type` — `single` | `start` | `in range` | `end` | `disable` | `current`
