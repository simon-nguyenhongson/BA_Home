import React from "react";
import { Toggle as Impl } from "./_impl.js";

/** Toggle — Figma layer "Toggle". See Toggle.prompt.md. */
export function Toggle(props) {
  return React.createElement(Impl, props);
}
