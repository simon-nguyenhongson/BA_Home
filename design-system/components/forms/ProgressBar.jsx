import React from "react";
import { ProgressBar as Impl } from "./_impl.js";

/** ProgressBar — Figma layer "Progress bar". See ProgressBar.prompt.md. */
export function ProgressBar(props) {
  return React.createElement(Impl, props);
}
