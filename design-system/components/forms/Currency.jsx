import React from "react";
import { Currency as Impl } from "./_impl.js";

/** Currency — Figma layer "Currency". See Currency.prompt.md. */
export function Currency(props) {
  return React.createElement(Impl, props);
}
