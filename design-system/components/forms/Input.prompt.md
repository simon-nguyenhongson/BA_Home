Single-line text field — default / hover / pressed / editable / non-editable / disabled.

The bare field; wrap it yourself when you need a label, or use ComboboxSingleSelect's label layouts as reference.

```jsx
<Input />
```

Props
- `iconLeft` — boolean
- `state` — `default` | `hover` | `pressed` | `editable` | `non editable` | `disabled`
- `iconRight` — boolean
