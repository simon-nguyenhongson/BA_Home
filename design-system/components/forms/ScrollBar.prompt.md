The custom scrollbar treatment applied to overflowing panels.

Apply to long dropdowns and drawers so the bar matches the system.

```jsx
<ScrollBar />
```

Props
- `type` — `horizontal` | `vertical`
- `length` — `25%` | `50%` | `75%`
