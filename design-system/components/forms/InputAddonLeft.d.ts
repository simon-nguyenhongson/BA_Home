import * as React from "react";

/**
 * Left-edge addon slot for a field.
 *
 * Source: Figma layer "input-addon-left" (node 9419:16646).
 */
export interface InputAddonLeftProps {
  className?: string;
  style?: React.CSSProperties;
  /** Swappable nested instance; defaults to the design's. */
  icon1?: React.ReactNode;
}

export declare function InputAddonLeft(props: InputAddonLeftProps): JSX.Element;
