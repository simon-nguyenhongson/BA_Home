import React from "react";
import { Input as Impl } from "./_impl.js";

/** Input — Figma layer "Input". See Input.prompt.md. */
export function Input(props) {
  return React.createElement(Impl, props);
}
