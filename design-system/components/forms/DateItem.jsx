import React from "react";
import { DateItem as Impl } from "./_impl.js";

/** DateItem — Figma layer "Date/item". See DateItem.prompt.md. */
export function DateItem(props) {
  return React.createElement(Impl, props);
}
