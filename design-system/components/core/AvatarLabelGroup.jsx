import React from "react";
import { AvatarLabelGroup as Impl } from "./_impl.js";

/** AvatarLabelGroup — Figma layer "Avatar label group". See AvatarLabelGroup.prompt.md. */
export function AvatarLabelGroup(props) {
  return React.createElement(Impl, props);
}
