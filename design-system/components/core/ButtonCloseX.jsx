import React from "react";
import { ButtonCloseX as Impl } from "./_impl.js";

/** ButtonCloseX — Figma layer "Button close X". See ButtonCloseX.prompt.md. */
export function ButtonCloseX(props) {
  return React.createElement(Impl, props);
}
