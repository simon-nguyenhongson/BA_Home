import React from "react";
import { Badge as Impl } from "./_impl.js";

/** Badge — Figma layer "Badge". See Badge.prompt.md. */
export function Badge(props) {
  return React.createElement(Impl, props);
}
