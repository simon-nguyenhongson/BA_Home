import React from "react";
import { Checkbox as Impl } from "./_impl.js";

/** Checkbox — Figma layer "Checkbox". See Checkbox.prompt.md. */
export function Checkbox(props) {
  return React.createElement(Impl, props);
}
