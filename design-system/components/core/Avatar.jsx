import React from "react";
import { Avatar as Impl } from "./_impl.js";

/** Avatar — Figma layer "Avatar". See Avatar.prompt.md. */
export function Avatar(props) {
  return React.createElement(Impl, props);
}
