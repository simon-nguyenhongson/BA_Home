import React from "react";
import { CheckboxIcon as Impl } from "./_impl.js";

/** CheckboxIcon — Figma layer "Checkbox icon". See CheckboxIcon.prompt.md. */
export function CheckboxIcon(props) {
  return React.createElement(Impl, props);
}
