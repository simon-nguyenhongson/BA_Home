Location trail with an optional go-back affordance and automatic collapse past 4 levels.

Use on detail pages reached from a list.

```jsx
<BreadcrumbsNormal />
```

Props
- `count` — `1` | `2` | `3` | `4` | `many`
- `goBack` — boolean
