import React from "react";
import { BreadcrumbsNormal as Impl } from "./_impl.js";

/** BreadcrumbsNormal — Figma layer "Breadcrumbs normal". See BreadcrumbsNormal.prompt.md. */
export function BreadcrumbsNormal(props) {
  return React.createElement(Impl, props);
}
