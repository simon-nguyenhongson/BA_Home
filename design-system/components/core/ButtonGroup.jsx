import React from "react";
import { ButtonGroup as Impl } from "./_impl.js";

/** ButtonGroup — Figma layer "Button group". See ButtonGroup.prompt.md. */
export function ButtonGroup(props) {
  return React.createElement(Impl, props);
}
