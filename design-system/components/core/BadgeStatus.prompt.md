Record status with a leading dot — success, processing, warning, error, default.

The default choice for a Status column in a table.

```jsx
<BadgeStatus />
```

Props
- `type` — `success` | `error` | `default` | `warning` | `processing`
