Checkbox with its label — unchecked / checked / indeterminate × 4 interaction states.

Label and box share one hit area; 8px gap between them.

```jsx
<CheckboxText />
```

Props
- `checked` — boolean
- `indeterminate` — boolean
- `type` — `checkbox` | `radio`
- `text` — boolean
- `state` — `default` | `disabled` | `focused` | `hover`
- `subtext` — boolean
