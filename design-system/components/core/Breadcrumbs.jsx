import React from "react";
import { Breadcrumbs as Impl } from "./_impl.js";

/** Breadcrumbs — Figma layer "Breadcrumbs". See Breadcrumbs.prompt.md. */
export function Breadcrumbs(props) {
  return React.createElement(Impl, props);
}
