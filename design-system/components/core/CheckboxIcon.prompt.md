The tick / dash glyph drawn inside a checkbox box.

Rarely used directly — reach for Checkbox or CheckboxText.

```jsx
<CheckboxIcon />
```

Props
- `checked` — boolean
- `indeterminate` — boolean
- `type` — `check circle` | `checkbox` | `radio`
- `state` — `default` | `disbaled` | `focused` | `hover`
