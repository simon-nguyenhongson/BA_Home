import React from "react";
import { BadgeRanking as Impl } from "./_impl.js";

/** BadgeRanking — Figma layer "Badge/Ranking". See BadgeRanking.prompt.md. */
export function BadgeRanking(props) {
  return React.createElement(Impl, props);
}
