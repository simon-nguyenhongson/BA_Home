import React from "react";
import { CheckboxText as Impl } from "./_impl.js";

/** CheckboxText — Figma layer "Checkbox text". See CheckboxText.prompt.md. */
export function CheckboxText(props) {
  return React.createElement(Impl, props);
}
