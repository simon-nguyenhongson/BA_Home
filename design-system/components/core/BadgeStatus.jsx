import React from "react";
import { BadgeStatus as Impl } from "./_impl.js";

/** BadgeStatus — Figma layer "Badge/Status". See BadgeStatus.prompt.md. */
export function BadgeStatus(props) {
  return React.createElement(Impl, props);
}
