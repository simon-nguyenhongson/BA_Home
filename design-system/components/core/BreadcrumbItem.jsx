import React from "react";
import { BreadcrumbItem as Impl } from "./_impl.js";

/** BreadcrumbItem — Figma layer "Breadcrumb item". See BreadcrumbItem.prompt.md. */
export function BreadcrumbItem(props) {
  return React.createElement(Impl, props);
}
