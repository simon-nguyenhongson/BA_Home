import React from "react";
import { CheckGroup as Impl } from "./_impl.js";

/** CheckGroup — Figma layer "Check group". See CheckGroup.prompt.md. */
export function CheckGroup(props) {
  return React.createElement(Impl, props);
}
