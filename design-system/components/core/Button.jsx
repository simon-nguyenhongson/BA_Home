import React from "react";
import { Button as Impl } from "./_impl.js";

/** Button — Figma layer "Button". See Button.prompt.md. */
export function Button(props) {
  return React.createElement(Impl, props);
}
