A segmented control for mutually-exclusive choices, rendered as one joined block.

Use for view switches (Day / Week / Month), not for unrelated actions.

```jsx
<ButtonGroup />
```

Props
- `icon` — `false` | `leading` | `only`
