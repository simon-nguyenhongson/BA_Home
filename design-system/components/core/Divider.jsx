import React from "react";
import { Divider as Impl } from "./_impl.js";

/** Divider — Figma layer "Divider". See Divider.prompt.md. */
export function Divider(props) {
  return React.createElement(Impl, props);
}
