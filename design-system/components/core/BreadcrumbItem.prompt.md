A single crumb — label plus separator.

Compose when you need a trail Breadcrumbs cannot express.

```jsx
<BreadcrumbItem />
```

Props
- `position` — `first` | `last` | `middle`
- `linked` — boolean
- `collapsed` — boolean
