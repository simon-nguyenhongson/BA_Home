import React from "react";
import { ButtonGroupItems as Impl } from "./_impl.js";

/** ButtonGroupItems — Figma layer "Button group items". See ButtonGroupItems.prompt.md. */
export function ButtonGroupItems(props) {
  return React.createElement(Impl, props);
}
