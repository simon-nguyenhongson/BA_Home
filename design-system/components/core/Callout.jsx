import React from "react";
import { Callout as Impl } from "./_impl.js";

/** Callout — Figma layer "Callout". See Callout.prompt.md. */
export function Callout(props) {
  return React.createElement(Impl, props);
}
