import React from "react";
import { Anchor as Impl } from "./_impl.js";

/** Anchor — Figma layer "Anchor". See Anchor.prompt.md. */
export function Anchor(props) {
  return React.createElement(Impl, props);
}
