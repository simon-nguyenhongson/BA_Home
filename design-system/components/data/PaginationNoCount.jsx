import React from "react";
import { PaginationNoCount as Impl } from "./_impl.js";

/** PaginationNoCount — Figma layer "Pagination no count". See PaginationNoCount.prompt.md. */
export function PaginationNoCount(props) {
  return React.createElement(Impl, props);
}
