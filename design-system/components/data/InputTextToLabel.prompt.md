Read-only text that becomes an input on edit.

For inline editing patterns in tables.

```jsx
<InputTextToLabel />
```

Props
- `required` — boolean
- `type` — `default` | `no label` | `label tooltip`
- `destructive` — boolean
- `state` — `default` | `hover` | `filled` | `focused` | `disabled`
