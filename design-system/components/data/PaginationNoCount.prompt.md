Table footer pagination — page size, current page, jump-to-page. States: default, load, only one.

Sits directly under the table card, full width.

```jsx
<PaginationNoCount />
```

Props
- `hada` — boolean
- `state` — `default` | `load` | `only one`
