import React from "react";
import { MetricChange as Impl } from "./_impl.js";

/** MetricChange — Figma layer "Metric/change". See MetricChange.prompt.md. */
export function MetricChange(props) {
  return React.createElement(Impl, props);
}
