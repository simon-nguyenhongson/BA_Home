import React from "react";
import { TextField as Impl } from "./_impl.js";

/** TextField — Figma layer "Text field". See TextField.prompt.md. */
export function TextField(props) {
  return React.createElement(Impl, props);
}
