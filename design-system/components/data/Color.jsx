import React from "react";
import { Color as Impl } from "./_impl.js";

/** Color — Figma layer "Color". See Color.prompt.md. */
export function Color(props) {
  return React.createElement(Impl, props);
}
