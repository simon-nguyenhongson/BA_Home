import React from "react";
import { NumberInput as Impl } from "./_impl.js";

/** NumberInput — Figma layer "Number input". See NumberInput.prompt.md. */
export function NumberInput(props) {
  return React.createElement(Impl, props);
}
