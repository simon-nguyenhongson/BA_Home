import React from "react";
import { Processbar as Impl } from "./_impl.js";

/** Processbar — Figma layer "Processbar". See Processbar.prompt.md. */
export function Processbar(props) {
  return React.createElement(Impl, props);
}
