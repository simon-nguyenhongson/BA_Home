import React from "react";
import { CellTitle as Impl } from "./_impl.js";

/** CellTitle — Figma layer "Cell Title". See CellTitle.prompt.md. */
export function CellTitle(props) {
  return React.createElement(Impl, props);
}
