import React from "react";
import { TableTitleText as Impl } from "./_impl.js";

/** TableTitleText — Figma layer "Table title text". See TableTitleText.prompt.md. */
export function TableTitleText(props) {
  return React.createElement(Impl, props);
}
