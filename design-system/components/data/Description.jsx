import React from "react";
import { Description as Impl } from "./_impl.js";

/** Description — Figma layer "Description". See Description.prompt.md. */
export function Description(props) {
  return React.createElement(Impl, props);
}
