import React from "react";
import { CellBodyNormal as Impl } from "./_impl.js";

/** CellBodyNormal — Figma layer "Cell Body Normal". See CellBodyNormal.prompt.md. */
export function CellBodyNormal(props) {
  return React.createElement(Impl, props);
}
