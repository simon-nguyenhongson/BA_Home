import React from "react";
import { InputTextToLabel as Impl } from "./_impl.js";

/** InputTextToLabel — Figma layer "Input text to label". See InputTextToLabel.prompt.md. */
export function InputTextToLabel(props) {
  return React.createElement(Impl, props);
}
