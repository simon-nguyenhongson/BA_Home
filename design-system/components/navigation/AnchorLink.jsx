import React from "react";
import { AnchorLink as Impl } from "./_impl.js";

/** AnchorLink — Figma layer "Anchor/Link". See AnchorLink.prompt.md. */
export function AnchorLink(props) {
  return React.createElement(Impl, props);
}
