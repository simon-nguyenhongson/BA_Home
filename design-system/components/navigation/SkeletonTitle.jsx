import React from "react";
import { SkeletonTitle as Impl } from "./_impl.js";

/** SkeletonTitle — Figma layer "Skeleton Title". See SkeletonTitle.prompt.md. */
export function SkeletonTitle(props) {
  return React.createElement(Impl, props);
}
