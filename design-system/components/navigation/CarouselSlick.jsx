import React from "react";
import { CarouselSlick as Impl } from "./_impl.js";

/** CarouselSlick — Figma layer "Carousel/Slick". See CarouselSlick.prompt.md. */
export function CarouselSlick(props) {
  return React.createElement(Impl, props);
}
