import React from "react";
import { Modal as Impl } from "./_impl.js";

/** Modal — Figma layer "Modal". See Modal.prompt.md. */
export function Modal(props) {
  return React.createElement(Impl, props);
}
