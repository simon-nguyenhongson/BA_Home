import React from "react";
import { SpinnerStatic as Impl } from "./_impl.js";

/** SpinnerStatic — Figma layer "Spinner / Static". See SpinnerStatic.prompt.md. */
export function SpinnerStatic(props) {
  return React.createElement(Impl, props);
}
