A nested submenu panel opened from a nav row.

Flyout to the right; keep nesting to two levels.

```jsx
<NavDropdownSubMenu />
```

Props
- `type` — `simple` | `simple 2-col` | `with recent 2` | `2-col` | `3-col` | `4-col`
- `breakpoint` — `desktop` | `mobile`
