import React from "react";
import { Collapse as Impl } from "./_impl.js";

/** Collapse — Figma layer "Collapse". See Collapse.prompt.md. */
export function Collapse(props) {
  return React.createElement(Impl, props);
}
