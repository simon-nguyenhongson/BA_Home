import React from "react";
import { StepBase as Impl } from "./_impl.js";

/** StepBase — Figma layer "Step base". See StepBase.prompt.md. */
export function StepBase(props) {
  return React.createElement(Impl, props);
}
