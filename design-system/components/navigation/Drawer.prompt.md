Side panel — overlay or push mode, with an optional action footer.

Use for record detail beside a list; Modal when the task must block.

```jsx
<Drawer />
```

Props
- `type` — `overlay` | `push`
- `tabs` — boolean
- `actionBtn` — boolean
