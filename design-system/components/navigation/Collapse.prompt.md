Expandable section with optional checkbox and trailing button.

For long forms and grouped record sections.

```jsx
<Collapse />
```

Props
- `checkBox` — boolean
- `button` — boolean
- `icon` — boolean
