The header's search input with its icon and clear affordance.

Global search only; use Input for in-form search.

```jsx
<SearchField />
```

Props
- `state` — `default` | `hover` | `filled` | `focused` | `disabled`
