import React from "react";
import { ModalButton as Impl } from "./_impl.js";

/** ModalButton — Figma layer "Modal button". See ModalButton.prompt.md. */
export function ModalButton(props) {
  return React.createElement(Impl, props);
}
