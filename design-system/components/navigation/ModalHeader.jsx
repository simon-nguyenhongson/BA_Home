import React from "react";
import { ModalHeader as Impl } from "./_impl.js";

/** ModalHeader — Figma layer "Modal header". See ModalHeader.prompt.md. */
export function ModalHeader(props) {
  return React.createElement(Impl, props);
}
