import React from "react";
import { HelpIcon as Impl } from "./_impl.js";

/** HelpIcon — Figma layer "Help icon". See HelpIcon.prompt.md. */
export function HelpIcon(props) {
  return React.createElement(Impl, props);
}
