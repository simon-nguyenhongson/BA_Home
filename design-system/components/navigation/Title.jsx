import React from "react";
import { Title as Impl } from "./_impl.js";

/** Title — Figma layer "Title". See Title.prompt.md. */
export function Title(props) {
  return React.createElement(Impl, props);
}
