import React from "react";
import { Cursor as Impl } from "./_impl.js";

/** Cursor — Figma layer "Cursor". See Cursor.prompt.md. */
export function Cursor(props) {
  return React.createElement(Impl, props);
}
