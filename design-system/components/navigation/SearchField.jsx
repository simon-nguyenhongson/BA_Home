import React from "react";
import { SearchField as Impl } from "./_impl.js";

/** SearchField — Figma layer "Search field". See SearchField.prompt.md. */
export function SearchField(props) {
  return React.createElement(Impl, props);
}
