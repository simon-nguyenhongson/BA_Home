import React from "react";
import { StepIndicator as Impl } from "./_impl.js";

/** StepIndicator — Figma layer ".📦Step / Indicator". See StepIndicator.prompt.md. */
export function StepIndicator(props) {
  return React.createElement(Impl, props);
}
