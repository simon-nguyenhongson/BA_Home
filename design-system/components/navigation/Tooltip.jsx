import React from "react";
import { Tooltip as Impl } from "./_impl.js";

/** Tooltip — Figma layer "Tooltip". See Tooltip.prompt.md. */
export function Tooltip(props) {
  return React.createElement(Impl, props);
}
