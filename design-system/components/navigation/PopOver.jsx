import React from "react";
import { PopOver as Impl } from "./_impl.js";

/** PopOver — Figma layer "Pop-over". See PopOver.prompt.md. */
export function PopOver(props) {
  return React.createElement(Impl, props);
}
