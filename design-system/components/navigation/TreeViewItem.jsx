import React from "react";
import { TreeViewItem as Impl } from "./_impl.js";

/** TreeViewItem — Figma layer "Tree view item". See TreeViewItem.prompt.md. */
export function TreeViewItem(props) {
  return React.createElement(Impl, props);
}
