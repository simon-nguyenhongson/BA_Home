import React from "react";
import { Alert as Impl } from "./_impl.js";

/** Alert — Figma layer "alert". See Alert.prompt.md. */
export function Alert(props) {
  return React.createElement(Impl, props);
}
