import React from "react";
import { NoData as Impl } from "./_impl.js";

/** NoData — Figma layer "No data". See NoData.prompt.md. */
export function NoData(props) {
  return React.createElement(Impl, props);
}
