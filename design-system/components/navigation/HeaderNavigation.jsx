import React from "react";
import { HeaderNavigation as Impl } from "./_impl.js";

/** HeaderNavigation — Figma layer "Header navigation". See HeaderNavigation.prompt.md. */
export function HeaderNavigation(props) {
  return React.createElement(Impl, props);
}
