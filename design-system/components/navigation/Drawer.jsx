import React from "react";
import { Drawer as Impl } from "./_impl.js";

/** Drawer — Figma layer "Drawer". See Drawer.prompt.md. */
export function Drawer(props) {
  return React.createElement(Impl, props);
}
