import React from "react";
import { Steps as Impl } from "./_impl.js";

/** Steps — Figma layer "Steps". See Steps.prompt.md. */
export function Steps(props) {
  return React.createElement(Impl, props);
}
