The hamburger / close toggle for the collapsed nav.

Marked private in Figma but published here because responsive layouts need it.

```jsx
<NavMenuButton />
```

Props
- `opened` — boolean
- `state` — `default` | `focused` | `hover`
