The product header — logo, 4 to 8 menu items, search, notifications, account.

One per page, 72px tall, sits above everything.

```jsx
<HeaderNavigation />
```

Props
- `type` — `4 menu` | `5 menu` | `6 menu` | `7 menu` | `8 menu` | `no menu`
- `breakpoint` — `desktop`
