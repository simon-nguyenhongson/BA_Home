import React from "react";
import { Menu as Impl } from "./_impl.js";

/** Menu — Figma layer "menu". See Menu.prompt.md. */
export function Menu(props) {
  return React.createElement(Impl, props);
}
