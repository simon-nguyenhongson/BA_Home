import React from "react";
import { StepItems as Impl } from "./_impl.js";

/** StepItems — Figma layer "Step items". See StepItems.prompt.md. */
export function StepItems(props) {
  return React.createElement(Impl, props);
}
