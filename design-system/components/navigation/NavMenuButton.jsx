import React from "react";
import { NavMenuButton as Impl } from "./_impl.js";

/** NavMenuButton — Figma layer "_Nav menu button". See NavMenuButton.prompt.md. */
export function NavMenuButton(props) {
  return React.createElement(Impl, props);
}
