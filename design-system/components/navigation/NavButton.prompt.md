A header-level navigation button with active and hover treatments.

For top-level destinations, not for actions.

```jsx
<NavButton />
```

Props
- `current` — boolean
- `size` — `md`
- `theme` — `default`
- `state` — `default` | `focused` | `hover`
- `showBadge` — boolean
