One step row inside a vertical stepper.

For side-rail flows such as an approval chain.

```jsx
<StepItems />
```

Props
- `size` — `md` | `xs`
- `position` — `first` | `last` | `middle`
- `status` — `danger` | `default / current` | `disabled` | `incomplete` | `success` | `warning`
