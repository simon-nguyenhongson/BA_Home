A step's dot plus its label and description block.

Compose when Steps' layout doesn't fit.

```jsx
<StepBase />
```

Props
- `status` — `danger` | `default / current` | `disabled` | `incomplete` | `success` | `warning`
- `size` — `md` | `sm` | `xs`
