The modal's title bar with title, optional supporting text and the close ✕.

Compose when building a modal body Modal doesn't cover.

```jsx
<ModalHeader />
```

Props
- `xClose` — boolean
- `type` — `sub text` | `only title`
- `paddingBorder` — boolean
