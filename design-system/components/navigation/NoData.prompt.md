Empty state — no data or no access, desktop and mobile.

Say what is missing and give one action.

```jsx
<NoData />
```

Props
- `type` — `illustration 02` | `no access` | `no data`
- `breakpoint` — `desktop` | `mobile`
