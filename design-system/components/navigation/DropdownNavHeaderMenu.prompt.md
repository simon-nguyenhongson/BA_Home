The multi-column mega-menu that drops out of the header.

Opens on the header item, not on hover of the whole bar.

```jsx
<DropdownNavHeaderMenu />
```

Props
- `open` — boolean
- `type` — `featured card` | `simple` | `simple 2-col`
- `breakpoint` — `desktop` | `mobile`
