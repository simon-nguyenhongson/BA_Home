import React from "react";
import { DropdownNavHeaderMenu as Impl } from "./_impl.js";

/** DropdownNavHeaderMenu — Figma layer "Dropdown nav header menu ". See DropdownNavHeaderMenu.prompt.md. */
export function DropdownNavHeaderMenu(props) {
  return React.createElement(Impl, props);
}
