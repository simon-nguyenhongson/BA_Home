import React from "react";
import { NavButton as Impl } from "./_impl.js";

/** NavButton — Figma layer "Nav button". See NavButton.prompt.md. */
export function NavButton(props) {
  return React.createElement(Impl, props);
}
